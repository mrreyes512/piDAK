galaxy_info:
  author: Mark Reyes
  description: A role to take a defaulted pi from zero to kubernetes
  license: BSD
  min_ansible_version: 2.4
  platforms:
    - name: Raspbian
  categories:
    - development
    - system

  galaxy_tags:
    - raspberry pi
    - docker
    - kubernetes

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
